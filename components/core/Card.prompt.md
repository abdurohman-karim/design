Glassmorphic container — the base surface for projects, stats, info panels.

```jsx
<Card hover glow padding="lg">
  <h3>uCash</h3>
  <p>Money-transfer & payments platform.</p>
</Card>
```

`hover` lifts and brightens the border; add `glow` for a white halo. `padding`: `sm` `md` `lg`.
