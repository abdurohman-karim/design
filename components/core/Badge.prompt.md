Tiny uppercase mono label for status / metadata.

```jsx
<Badge dot>Available for work</Badge>
<Badge variant="outline">2025</Badge>
<Badge variant="solid">Fintech</Badge>
```

`dot` adds a glowing status dot. Variants: `default` (glass), `outline`, `solid` (white).
